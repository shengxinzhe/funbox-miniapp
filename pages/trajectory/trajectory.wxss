/* ==============================
   Base
============================== */
.page {
  min-height: 100vh;
  background: #F5F5F7;
  opacity: 0;
  transition: opacity 0.4s ease;
}
.fade-in { opacity: 1; }

/* ==============================
   Home Screen
============================== */
.home-screen {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 56rpx 40rpx 60rpx;
  min-height: 100vh;
  box-sizing: border-box;
}

.home-icon-wrap {
  margin-bottom: 32rpx;
}
.home-icon {
  width: 160rpx;
  height: 160rpx;
  background: linear-gradient(135deg, #5856D6, #AF52DE);
  border-radius: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  box-shadow: 0 12rpx 40rpx rgba(88, 86, 214, 0.35);
}
.cannon-icon {
  width: 60rpx;
  height: 24rpx;
  background: rgba(255,255,255,0.9);
  border-radius: 12rpx;
  position: absolute;
  left: 28rpx;
  bottom: 52rpx;
  transform: rotate(-40deg);
  transform-origin: left center;
}
.trajectory-arc {
  width: 80rpx;
  height: 40rpx;
  border: 4rpx dashed rgba(255,255,255,0.6);
  border-radius: 50% 50% 0 0;
  border-bottom: none;
  position: absolute;
  bottom: 28rpx;
  left: 40rpx;
}

.home-title {
  font-size: 56rpx;
  font-weight: 800;
  color: #1D1D1F;
  letter-spacing: -1rpx;
}
.home-subtitle {
  font-size: 28rpx;
  font-weight: 500;
  color: #5856D6;
  margin-top: 8rpx;
}
.home-desc {
  font-size: 26rpx;
  color: #86868B;
  line-height: 1.7;
  text-align: center;
  margin-top: 24rpx;
  padding: 0 16rpx;
}

.home-best {
  display: flex;
  align-items: baseline;
  gap: 8rpx;
  margin-top: 36rpx;
  background: #FFFFFF;
  padding: 24rpx 48rpx;
  border-radius: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
}
.best-label {
  font-size: 24rpx;
  color: #86868B;
}
.best-score {
  font-size: 52rpx;
  font-weight: 800;
  color: #5856D6;
  font-variant-numeric: tabular-nums;
}
.best-unit {
  font-size: 24rpx;
  color: #AEAEB2;
}

.home-rules {
  width: 100%;
  background: #FFFFFF;
  border-radius: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
  padding: 32rpx 36rpx;
  margin-top: 32rpx;
  box-sizing: border-box;
}
.rule-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  font-size: 26rpx;
  color: #1D1D1F;
  padding: 10rpx 0;
}
.rule-dot {
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #5856D6, #AF52DE);
  flex-shrink: 0;
}

.start-btn {
  margin-top: 48rpx;
  background: linear-gradient(135deg, #5856D6, #AF52DE);
  color: #FFFFFF;
  font-size: 34rpx;
  font-weight: 700;
  padding: 28rpx 0;
  width: 100%;
  text-align: center;
  border-radius: 48rpx;
  box-shadow: 0 8rpx 32rpx rgba(88, 86, 214, 0.35);
  letter-spacing: 2rpx;
}
.start-btn:active {
  opacity: 0.85;
  transform: scale(0.97);
}

/* ==============================
   Game Screen
============================== */
.game-screen {
  display: flex;
  flex-direction: column;
  background: #F5F5F7;
  min-height: 100vh;
}

.game-hud {
  display: flex;
  align-items: center;
  justify-content: center;
  background: #FFFFFF;
  padding: 20rpx 40rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
  gap: 40rpx;
}
.hud-round, .hud-score-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
}
.hud-label {
  font-size: 22rpx;
  color: #86868B;
  font-weight: 500;
}
.hud-value {
  font-size: 36rpx;
  font-weight: 800;
  color: #1D1D1F;
  font-variant-numeric: tabular-nums;
}
.hud-value.accent {
  color: #5856D6;
}
.hud-divider {
  width: 2rpx;
  height: 48rpx;
  background: #E5E5EA;
}

.canvas-wrap {
  position: relative;
  margin: 24rpx auto;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.10);
}
.traj-canvas {
  display: block;
  background: #FFFFFF;
}

.canvas-hint {
  position: absolute;
  top: 16rpx;
  left: 50%;
  transform: translateX(-50%);
  background: rgba(88, 86, 214, 0.12);
  border-radius: 20rpx;
  padding: 10rpx 28rpx;
  pointer-events: none;
}
.hint-text {
  font-size: 24rpx;
  color: #5856D6;
  font-weight: 600;
  white-space: nowrap;
}

.score-popup {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.5);
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 28rpx 48rpx;
  text-align: center;
  box-shadow: 0 8rpx 40rpx rgba(0,0,0,0.16);
  opacity: 0;
  transition: opacity 0.2s ease, transform 0.2s ease;
  pointer-events: none;
}
.score-popup.visible {
  opacity: 1;
  transform: translate(-50%, -50%) scale(1);
}
.popup-pts {
  font-size: 56rpx;
  font-weight: 900;
  font-variant-numeric: tabular-nums;
}
.pts-great { color: #34C759; }
.pts-good  { color: #FF9500; }
.pts-ok    { color: #86868B; }
.popup-dist {
  font-size: 24rpx;
  color: #86868B;
  margin-top: 6rpx;
}

.action-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 40rpx 40rpx;
  gap: 24rpx;
}
.action-hint {
  flex: 1;
  font-size: 26rpx;
  color: #86868B;
  line-height: 1.4;
}
.fire-btn {
  flex-shrink: 0;
  font-size: 32rpx;
  font-weight: 700;
  padding: 24rpx 56rpx;
  border-radius: 48rpx;
  text-align: center;
  letter-spacing: 2rpx;
  transition: opacity 0.2s;
}
.fire-btn.active {
  background: linear-gradient(135deg, #5856D6, #AF52DE);
  color: #FFFFFF;
  box-shadow: 0 6rpx 24rpx rgba(88, 86, 214, 0.35);
}
.fire-btn.active:active {
  opacity: 0.8;
  transform: scale(0.97);
}
.fire-btn.disabled {
  background: #E5E5EA;
  color: #AEAEB2;
}

/* ==============================
   Result Screen
============================== */
.result-screen {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 60rpx 40rpx 60rpx;
  min-height: 100vh;
  box-sizing: border-box;
}

.result-header {
  text-align: center;
  margin-bottom: 40rpx;
}
.result-title {
  font-size: 48rpx;
  font-weight: 800;
  color: #1D1D1F;
}
.result-subtitle {
  font-size: 28rpx;
  font-weight: 600;
  margin-top: 8rpx;
  padding: 8rpx 28rpx;
  border-radius: 20rpx;
  display: inline-block;
}
.grade-s { color: #5856D6; background: rgba(88,86,214,0.10); }
.grade-a { color: #34C759; background: rgba(52,199,89,0.10); }
.grade-b { color: #FF9500; background: rgba(255,149,0,0.10); }
.grade-c { color: #86868B; background: rgba(134,134,139,0.10); }

.score-card {
  width: 100%;
  background: #FFFFFF;
  border-radius: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
  padding: 40rpx;
  box-sizing: border-box;
  text-align: center;
}
.score-main {
  display: flex;
  align-items: baseline;
  justify-content: center;
  gap: 8rpx;
}
.score-num {
  font-size: 96rpx;
  font-weight: 900;
  color: #5856D6;
  font-variant-numeric: tabular-nums;
  line-height: 1;
}
.score-max {
  font-size: 28rpx;
  color: #AEAEB2;
  font-weight: 500;
}
.score-bar-wrap {
  margin-top: 24rpx;
}
.score-bar-bg {
  width: 100%;
  height: 12rpx;
  background: #F0F0F5;
  border-radius: 6rpx;
  overflow: hidden;
}
.score-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, #5856D6, #AF52DE);
  border-radius: 6rpx;
  transition: width 1s ease;
}
.score-pct {
  font-size: 24rpx;
  color: #86868B;
  margin-top: 12rpx;
}

.stats-row {
  display: flex;
  gap: 24rpx;
  width: 100%;
  margin-top: 24rpx;
}
.stat-box {
  flex: 1;
  background: #FFFFFF;
  border-radius: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
  padding: 28rpx 0;
  text-align: center;
}
.stat-n {
  font-size: 44rpx;
  font-weight: 800;
  color: #1D1D1F;
  font-variant-numeric: tabular-nums;
}
.stat-n.accent { color: #5856D6; }
.stat-l {
  font-size: 22rpx;
  color: #86868B;
  margin-top: 6rpx;
}

.new-best {
  margin-top: 24rpx;
  font-size: 28rpx;
  font-weight: 700;
  color: #5856D6;
  background: rgba(88,86,214,0.10);
  padding: 12rpx 40rpx;
  border-radius: 20rpx;
  animation: popIn 0.4s cubic-bezier(0.17,0.89,0.32,1.28);
}
@keyframes popIn {
  0%   { transform: scale(0); opacity: 0; }
  100% { transform: scale(1); opacity: 1; }
}

.result-actions {
  display: flex;
  gap: 24rpx;
  width: 100%;
  margin-top: 40rpx;
}
.retry-btn, .home-btn {
  flex: 1;
  text-align: center;
  font-size: 30rpx;
  font-weight: 700;
  padding: 28rpx 0;
  border-radius: 48rpx;
}
.retry-btn {
  background: linear-gradient(135deg, #5856D6, #AF52DE);
  color: #FFFFFF;
  box-shadow: 0 6rpx 24rpx rgba(88, 86, 214, 0.30);
}
.retry-btn:active { opacity: 0.85; transform: scale(0.97); }
.home-btn {
  background: #FFFFFF;
  color: #5856D6;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.08);
}
.home-btn:active { opacity: 0.85; }

.share-btn {
  width: 100%;
  background: #FFFFFF !important;
  color: #5856D6 !important;
  font-size: 28rpx !important;
  font-weight: 600 !important;
  border-radius: 48rpx !important;
  margin-top: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.08);
  padding: 0;
  line-height: 88rpx;
  min-height: 88rpx;
}
.share-btn::after { border: none; }
